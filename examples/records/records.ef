module Records {
    type Vehicle = record {
        name: String,
        wheelCount: Int
    }

    let car = Vehicle {
        name: "Test Car",
        wheelCount: 4
    }

    assert (car.wheelCount == 4)
    debug car.name
}

debug Records.car.name